apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-echo707a2dad-80f8-4962-a81a-65714d4adb0c.fullname" . }}
  labels:
    {{- include "go-echo707a2dad-80f8-4962-a81a-65714d4adb0c.labels" . | nindent 4 }}
  annotations:
    {{ toYaml .Values.service.annotations | nindent 4 }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: svchttp
  selector:
    {{- include "go-echo707a2dad-80f8-4962-a81a-65714d4adb0c.selectorLabels" . | nindent 6 }}
