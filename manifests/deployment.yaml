apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-echo
  labels:
    app.kubernetes.io/name: go-echo
    kubernetes.azure.com/generator: devhub
  namespace: devhub-pvt-cluster-e2e-test-id-5
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: go-echo
  template:
    metadata:
      labels:
        app.kubernetes.io/name: go-echo
    spec:
      containers:
        - name: go-echo
          image: devhube2ea6019d18ad6b420bb61a38859d5db892.azurecr.io/templateimage:latest:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 1323
          resources:
            requests:
              cpu: "0.5"
              memory: "0.5Gi"
            limits:
              cpu: "1"
              memory: "1Gi"
          envFrom:
            - configMapRef:
                name: go-echo-config
            - secretRef:
                name: secret-ref
                optional: true
          livenessProbe:
            tcpSocket:
              port: 1323
          readinessProbe:
            tcpSocket:
              port: 1323
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 1
            successThreshold: 1
            initialDelaySeconds: 3
          startupProbe:
            tcpSocket:
              port: 1323
            periodSeconds: 10
            timeoutSeconds: 1
            failureThreshold: 3
            successThreshold: 1
            initialDelaySeconds: 0
          securityContext:
            seccompProfile:
              type: RuntimeDefault
            capabilities:
              drop:
                - ALL
              add:
                - SETPCAP
                - MKNOD
                - AUDIT_WRITE
                - CHOWN
                - DAC_OVERRIDE
                - FOWNER
                - FSETID
                - KILL
                - SETGID
                - SETUID
                - NET_BIND_SERVICE
                - SYS_CHROOT
                - SETFCAP
                - SYS_PTRACE
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              topologyKey: kubernetes.io/hostname
              labelSelector:
                matchLabels:
                  app.kubernetes.io/name: go-echo
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: go-echo